#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_grunt-strip@0.2.1@grunt-strip/bin/grunt-strip" "$@"
  ret=$?
else 
  node  "$basedir/../_grunt-strip@0.2.1@grunt-strip/bin/grunt-strip" "$@"
  ret=$?
fi
exit $ret
